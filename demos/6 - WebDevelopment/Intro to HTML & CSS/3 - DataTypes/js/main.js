// 1. declare variables for each type
var num = 10;
var str = num + ' is my favorite number.';
var nullData = null;
var undefinedData = '';

// Symbols allow us to create "hidden" properties of an object, that no other part of code
